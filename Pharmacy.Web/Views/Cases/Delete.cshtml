@using Pharmacy.Core.Entities.ViewModels.Responses.Cases
@model DisplayCase

@{
    ViewData["Title"] = "Delete";
}

<div class="container">
    <h1 style="text-align:center"><span class="Display-4 text-danger">Are you sure you want to delete this case? </span></h1>
    <div class="owl-prev text-center" style="font-size: 24px;">
        <a asp-action="Index"><span class="icon-arrow_back"></span></a>
    </div>

    <div class="card mb-3">
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-2" style="color: black; border-right: 1px solid #ccc; padding-right: 10px;">
                    @Html.DisplayNameFor(model => model.Name)
                </dt>
                <dd class="col-sm-10" style="color: black;">
                    @Html.DisplayFor(model => model.Name)
                </dd>
            </dl>
            <hr />
            <dl class="row">
                <dt class="col-sm-2" style="color: black; border-right: 1px solid #ccc; padding-right: 10px;">
                    @Html.DisplayNameFor(model => model.PatientFullName)
                </dt>
                <dd class="col-sm-10" style="color: black;">
                    @Html.DisplayFor(model => model.PatientFullName)
                </dd>
            </dl>
        </div>
    </div>

    <div class="text-center">
        <div class="form-group">
            <form asp-action="ConfirmDelete" asp-route-id="@Model.Id">
                <button type="submit" class="btn btn-outline-danger">Delete</button>
                <a class="btn btn-outline-secondary" asp-action="Index">Go Back</a>

            </form>
        </div>
    </div>

</div>